spring:
  session:
    store-type: hash_map
security:
  user:
    password: secret
    name: user
  http.multipart:
    max-file-size: 50MB
    max-request-size: 51MB

files:
  listing.baseDirectory: "C:\\Users\\joel\\Development\\Kotlin\\pegnu-files"
  download.baseUrl: http://localhost:8080
  application.base-url: http://localhost:8080